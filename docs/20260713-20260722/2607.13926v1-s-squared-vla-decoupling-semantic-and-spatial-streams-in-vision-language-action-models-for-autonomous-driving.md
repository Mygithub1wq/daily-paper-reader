---
title: "S-squared-VLA: Decoupling Semantic and Spatial Streams in Vision-Language-Action Models for Autonomous Driving"
title_zh: S-squared-VLA：自动驾驶视觉-语言-动作模型中语义流与空间流的解耦
authors: "Jianguo Yu, Rukang Wang, Duanfeng Chu, Chen Wang, Renju Feng, Liping Lu"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.13926v1"
tags: ["query:embodied-vla"]
score: 9.0
evidence: 用于自动驾驶的视觉语言动作模型，解耦语义和空间流
tldr: "针对视觉-语言-动作(VLA)模型在自动驾驶中因语义-物理差距导致空间表征崩塌的问题，提出S-squared-VLA框架，通过解耦语义与空间流，分别利用分层桥接提取多尺度特征和保留未压缩空间先验，再经双流规划适配器融合。在NAVSIM闭环基准上，以纯监督微调取得PDMS 87.1和碰撞率98.4%的新最佳，验证了缓解空间信息退化对安全导航的关键作用。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.13926v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 912, \"height\": 1020, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13926v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1877, \"height\": 869, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13926v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 920, \"height\": 740, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.13926v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 567, \"height\": 506, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13926v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1537, \"height\": 542, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13926v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1626, \"height\": 226, \"label\": \"Table\"}]"
motivation: 标准VLA模型因语言离散与轨迹连续的语义-物理差距，导致空间表征崩溃，无法生成精确控制动作。
method: 显式解耦语义与空间流；语义流用分层桥接提取多尺度VLM特征，空间流绕过自回归瓶颈保留原始视觉空间先验，经双流注意力适配器融合。
result: "在NAVSIM上PDMS达87.1，碰撞率98.4%，均为VLA模型在纯SFT设定下的最佳结果。"
conclusion: 通过缓解空间表征崩塌，S-squared-VLA显著超越现有VLA基线，验证了空间流独立保留对安全驾驶的必要性。
---

## 摘要
视觉语言模型（VLM）在自动驾驶中的高层推理方面展现了巨大潜力，但其根本上难以生成精确的低层控制动作。这一局限源于离散语言标记与连续轨迹规划之间的固有失配所导致的语义-物理鸿沟。尽管视觉-语言-动作（VLA）架构试图通过将感知与控制统一为单一策略来弥补这一鸿沟，但这种纠缠带来了新的瓶颈。标准VLA模型会出现严重的空间表示崩溃，不可逆地退化了对安全、边界感知导航至关重要的细粒度空间和几何先验。为解决此局限，我们提出S-squared-VLA，其明确解耦了视觉-语言-动作模型中的语义流与空间流。语义流利用分层桥接提取多尺度VLM特征以进行鲁棒的意图推理。与此同时，独立的空间流绕过自回归语言瓶颈，直接保留来自视觉编码器的未压缩空间特征。通过集成辅助感知监督，该流明确赋予模型丰富的空间和几何先验。最后，双流规划适配器通过级联注意力机制将高层语义意图与精确空间约束相融合。在NAVSIM闭环基准上的评估表明，S-squared-VLA在纯监督微调（SFT）设置下实现了87.1的预测驾驶模型分数（PDMS），刷新了VLA模型的最优性能。通过缓解传统VLM的空间表示崩溃，我们的框架显著优于基线，在所有评估方法中实现了最高的无碰撞率（NC）98.4。

## Abstract
Vision-Language Models (VLMs) have demonstrated remarkable potential for high-level reasoning in autonomous driving, yet they fundamentally struggle to generate precise, low-level control actions. This limitation is rooted in a semantic-physical gap caused by the inherent mismatch between discrete language tokens and continuous trajectory planning. While Vision-Language-Action (VLA) architectures attempt to bridge this gap by unifying perception and control into a single policy, this entanglement creates a new bottleneck. Standard VLAs experience a severe spatial representation collapse, which irreversibly degrades the fine-grained spatial and geometric priors essential for safe, boundary-aware navigation. To address this limitation, we propose the S-squared-VLA, which explicitly decouples the semantic and spatial streams in Vision-Language-Action models. The semantic stream leverages hierarchical bridging to extract multi-scale VLM features for robust intent reasoning. In parallel, an independent spatial stream bypasses the autoregressive language bottleneck, directly preserving uncompressed spatial features from the visual encoder. By integrating auxiliary perception supervision, this stream explicitly equips the model with rich spatial and geometric priors. Finally, a dual-stream planning adapter fuses high-level semantic intent with precise spatial constraints via cascaded attention mechanisms. Evaluations on the NAVSIM closed-loop benchmark show that S-squared-VLA achieves a Predictive Driver Model Score (PDMS) of 87.1, establishing a new state-of-the-art for VLA models under a purely supervised fine-tuning (SFT) setting. By mitigating the spatial representation collapse of traditional VLMs, our framework significantly outperforms baselines, achieving the highest No Collision (NC) rate of 98.4 among all evaluated methods.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有视觉-语言-动作（VLA）模型在自动驾驶中面临严重的**语义-物理鸿沟**：
  - VLMs 擅长高层语义推理，但输出离散语言标记（如“左转”），无法生成连续、精确的轨迹控制信号。
  - VLA 模型虽尝试统一感知与控制，但标准单流架构将语义推理与空间感知纠缠在一起，导致**空间表征崩溃**（spatial representation collapse），即深度网络压缩过程不可逆地丢失细粒度空间与几何先验，使生成的轨迹不安全、不遵守几何边界。
- **研究动机**：需要一种能够**同时保留高层语义意图和低层空间精确性**的架构，以生成安全、边界感知、物理可行的驾驶轨迹。
- **整体含义**：本文提出语义流与空间流显式解耦的 VLA 框架（S²-VLA），从根源上缓解空间信息退化，实现闭环规划性能的新突破。

### 2. 论文提出的方法论

- **核心思想**：将 VLA 模型分解为两个独立的处理流：
  - **语义流**：利用预训练 VLM（InternVL3-2B）提取多尺度高层语义特征，用于意图推理。
  - **空间流**：绕过自回归语言瓶颈，直接从视觉编码器（ViT）获取未压缩的密集空间特征，并通过辅助感知任务（BEV 地图预测、动态目标检测）注入显式空间与几何先验。
  - 最后通过**双流规划适配器**（Dual-Stream Planning Adapter）将语义意图与空间约束融合，生成轨迹。
- **关键技术细节**：
  1. **多尺度语义流**：
     - 输入：导航命令、自车运动历史、前视图图像、可学习动作查询（Nact=64）。
     - 骨干网络：InternVL3-2B，采用稀疏多尺度采样策略，提取特定层（第3,8,13,18,23,24层）的隐藏状态，形成多尺度特征集。
     - 将动作查询与自车状态编码（MLP）拼接，输出语义特征和动作中心特征。
  2. **任务驱动空间流**：
     - 动态分辨率策略：将前视图分成9个patch（8个局部+1个全局缩略图），每个patch加入64个可学习视觉查询，通过ViT自注意力聚合空间细节。
     - 两个辅助感知头：
       - **地图头**：将1D token序列重塑为2D网格，通过上采样+卷积输出局部BEV语义地图（范围0-32m x ±32m），使用加权交叉熵损失训练，强调可行驶区域和车道边界。
       - **智能体头**：基于DETR范式，用30个可学习查询+Transformer解码器预测其他车辆的边界框和分类，使用匈牙利匹配和L1回归+BCE损失。
     - 最终输出密集的几何特征 Vspatial。
  3. **双流规划适配器**：
     - 初始化8个可学习规划令牌（对应未来8个路径点）。
     - N个堆叠块，每个块分两阶段：
       1. **语义和状态对齐**：用规划令牌作为查询，对多尺度语义特征 Vsem 和自车记忆特征 Emem 做并行交叉注意力，可学习门控参数 tanh(g) 调节贡献，融合得到 Pfuse。
       2. **视觉空间精修**：以 Pfuse 为查询，对空间特征 Vspatial 做交叉注意力，再通过FFN得到精炼令牌。
     - MLP解码器输出轨迹点 (M×3)，使用L1回归损失+平滑约束（加速度、加加速度的Smooth L1损失）。
  4. **总体损失**：Ltotal = λplan Lplan + λagent Lagent + λmap Lmap，λ分别为1.0, 0.1, 0.5。

### 3. 实验设计

- **数据集和场景**：NAVSIM benchmark，强调闭环交互。评价指标为**预测驾驶模型分数（PDMS）**，由五个子指标合成：无碰撞率（NC）、可行驶区域合规性（DAC）、碰撞时间（TTC）、舒适性（Comf）、自车进度（EP）。
- **对比方法**：
  - **E2E模型**：TransFuser、UniAD、PARA-Drive、DRAMA、ARTEMIS、DiffusionDrive（部分使用LiDAR）。
  - **VLM/VLA模型**：InternVL3-2B（基础VLM基线）、ImagiDrive、AutoVLA、ReCogDrive。
- **公平性**：统一在纯监督微调（SFT）设置下比较，排除后训练强化学习（如GRPO）的增强版本。

### 4. 资源与算力

- **文中明确说明**：使用 **4块 NVIDIA A100 GPU**，优化器为 AdamW，学习率 1×10⁻⁴。
- **训练阶段**：
  - 阶段1（SFT）：在ReCogDrive VQA数据集上微调VLM 3轮。
  - 阶段2（冻结VLM，使用LoRA）：训练意图提取和感知模块 4轮。
  - 阶段3：冻结感知和意图部分，微调视觉精修模块 4轮。
- 总训练轮次为3+4+4=11轮，但未给出每轮耗时或总GPU时数。资源描述基本充分。

### 5. 实验数量与充分性

- **定量对比**：主表（表II）共对比了10种方法（含基线），并给出全部6项指标。消融实验（表III）包含4组变体：BaseVLM → 加语义特征 → 加空间流 → 加辅助感知，逐步验证各组件贡献。
- **定性分析**：图4展示了与Transfuser、InternVL3-2B、ReCogDrive的轨迹对比。
- **充分性**：实验覆盖了主要竞争对手，消融清晰，子指标分解合理。但缺少对同一骨干网络下不同解耦策略的消融（如只改变注意力顺序），也未展示不同LoRA秩或查询数量的敏感性分析。总体较充分，但可进一步扩展超参数实验。

### 6. 论文的主要结论与发现

- S²-VLA在NAVSIM上达到PDMS **87.1**，**无碰撞率98.4%**，均为纯SFT设定下VLA模型的最佳性能，且超越多数带LiDAR的E2E模型。
- 显式解耦语义流与空间流能有效**缓解空间表征崩塌**：空间流保留未压缩的几何特征，辅助感知任务提供显式空间先验，使轨迹更安全、更贴合道路边界。
- 双流规划适配器的级联设计（先语义对齐再空间精修）优于并行或混叠方案。
- 验证了语言离散化导致的几何偏差（VLM基线在弯道偏出车道线，红色圆圈标记），而S²-VLA维持车道居中。

### 7. 优点

- **架构创新**：将语义推理与空间感知解耦，既保留了VLM的高层推理能力，又避免了空间信息被语言瓶颈压缩，是解决VLA空间退化的有效方案。
- **无昂贵传感器**：仅使用单目前视摄像头，性能即可媲美甚至超越融合LiDAR的方法，降低了硬件需求。
- **纯监督训练**：在SFT设定下达到SOTA，证明不需要强化学习后训练即可实现高性能，训练稳定性好。
- **设计简洁有效**：稀疏多尺度特征采样、可学习查询、双流适配器级联注意力等模块设计合理，消融验证了每一步的必要性。
- **安全指标突出**：最高无碰撞率表明模型对障碍物和道路边界感知精确，具有实用价值。

### 8. 不足与局限

- **计算开销**：密集的空间特征提取和双流融合带来显著计算负担，作者也承认需要未来开发更稀疏的表示机制。
- **实验覆盖**：仅在NAVSIM单一基准上测试，缺乏在其他数据集（如nuScenes、Waymo Open）上的泛化验证；也未展示真实世界道路测试或不同天气/光照条件下的鲁棒性。
- **超参数敏感度未知**：动作查询数量（64）、采样层选择（6层）等关键超参数未见敏感性分析。
- **与强化学习的兼容性**：论文指出双流架构与训练范式正交，但未实际验证经RL微调后的效果；也未与其他使用RL的VLA变体进行公平对比（仅排除了它们的RL增强版）。
- **自动驾驶之外的限制**：方法针对自动驾驶设计，迁移到通用机器人操作等其他VLA任务（如物体抓取）可能需要调整感知头。
- **风险偏差**：训练数据来自NAVSIM可能引入特定驾驶风格偏差，对长尾场景（如无车道线乡间路、极端天气）的表现未评估。

（完）
