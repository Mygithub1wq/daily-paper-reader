---
title: "TurboVLA: Real-Time Vision-Language-Action Model at 32 Hz on an RTX 4090 with <1 GB VRAM"
title_zh: TurboVLA：在RTX 4090上以32 Hz运行、VRAM占用小于1 GB的实时视觉-语言-动作模型
authors: "Hengyi Xie, Chenfei Yao, Xianjin Wu, Xuanyang Xi, Yiping Tang, Di Xu, Yingying Zhu, Dingkang Liang, Xiang Bai, Han Ding"
date: 2026-07-29
pdf: "https://arxiv.org/pdf/2607.27205v1"
tags: ["query:embodied-vla"]
score: 10.0
evidence: 新VLA范式直接映射V+L到A，实现实时性能
tldr: "现有的视觉-语言-动作模型采用LLM为中心的V→L→A路线，导致计算和内存开销大。本文提出TurboVLA，将路线改为直接的V+L→A映射，通过轻量双向视觉-语言交互独立编码信息，并用紧凑解码器预测动作。在LIBERO上以0.2B参数、31.2ms推理延迟、0.9GB显存实现97.7%平均成功率，匹配甚至超越更大模型，为高效机器人操作提供了新视角。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.27205v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1273, \"height\": 635, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.27205v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1406, \"height\": 793, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.27205v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1383, \"height\": 851, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.27205v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1453, \"height\": 532, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.27205v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 575, \"height\": 405, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.27205v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1457, \"height\": 1075, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.27205v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1457, \"height\": 619, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.27205v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 721, \"height\": 184, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.27205v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 719, \"height\": 205, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.27205v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 718, \"height\": 184, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.27205v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 719, \"height\": 206, \"label\": \"Table\"}]"
motivation: 降低VLA模型在每次策略调用时的巨大计算和内存开销，避免以LLM为中心的设计瓶颈。
method: 独立编码视觉和语言，通过轻量双向交互直接映射到动作，使用紧凑解码器预测连续动作块。
result: "在RTX 4090上以0.2B参数、31.2ms延迟、0.9GB显存实现LIBERO 97.7%平均成功率。"
conclusion: TurboVLA作为简单高效的VLA替代方案，展示了视觉、语言和动作连接的新方向。
---

## 摘要
视觉-语言-动作（VLA）模型通常采用以LLM为中心的V→L→A路径，其中视觉观测被投影到大语言模型的表示空间中，然后再解码为机器人动作。尽管这种设计有效，但在每次策略调用时会带来大量的计算和内存开销。在这项工作中，我们提出了TurboVLA，一种新的VLA范式，它将传统的V→L→A路径重新表述为直接的V+L→A映射。TurboVLA不使用大语言模型作为感知和动作之间的中央接口，而是独立编码视觉观测和语言指令，通过轻量级双向视觉-语言交互直接在它们之间交换信息，并使用紧凑的解码器预测连续动作块。这种简单设计直接从视觉和语言特征构建任务条件表示，显著降低了VLA推理的计算和内存成本。在LIBERO上，TurboVLA仅用0.2B参数、31.2毫秒推理延迟和0.9 GB推理显存（在消费级RTX 4090上）就达到了97.7%的平均成功率，匹配或超越了更大规模的VLA策略。这些结果确立了TurboVLA作为当前以LLM为中心的VLA范式的一种简单而有效的替代方案，为如何将视觉、语言和动作连接起来实现高效机器人操作提供了新视角。代码可在https://github.com/H-EmbodVis/TurboVLA获取。

## Abstract
Vision-language-action (VLA) models commonly adopt an LLM-centric $V \to L \to A$ pathway, where visual observations are projected into the representation space of a large language model before being decoded into robot actions. Although effective, this design incurs substantial computation and memory overhead at every policy invocation. In this work, we introduce TurboVLA, a new VLA paradigm that reformulates the conventional $V \to L \to A$ pathway as a direct $V + L \to A$ mapping. Instead of using a large language model as the central interface between perception and action, TurboVLA independently encodes visual observations and language instructions, directly exchanges information between them through lightweight bidirectional vision-language interaction, and predicts continuous action chunks with a compact decoder. This simple design constructs task-conditioned representations directly from visual and linguistic features, significantly reducing the computational and memory costs of VLA inference. On LIBERO, TurboVLA achieves 97.7% average success with only 0.2B parameters, 31.2 ms inference latency, and 0.9 GB inference VRAM on a consumer-grade RTX 4090, matching or outperforming substantially larger VLA policies. These results establish TurboVLA as a simple and effective alternative to the prevailing LLM-centric VLA paradigm, offering a new perspective on how vision, language, and action can be connected for efficient robotic manipulation. Code is available at https://github.com/H-EmbodVis/TurboVLA.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：现有的视觉-语言-动作（VLA）模型普遍采用以大型语言模型（LLM）为中心的 $V \to L \to A$ 路径。视觉观测被投影到 LLM 的表示空间，再与指令共同处理，最后解码为动作。这种设计虽然有效，但每次策略调用都产生巨大的计算和内存开销，导致推理延迟高、控制频率低，难以在资源受限的机器人平台上实现实时执行。  
- **核心问题**：是否必须让执行级控制始终以 LLM 为中心？能否设计一种直接映射视觉和语言到动作的轻量级 VLA 范式，从而显著降低推理延迟和显存占用，同时保持强大的操作性能？  
- **整体含义**：论文提出 **TurboVLA**，一种新的 $V + L \to A$ 直接映射范式，将 L（语言模型）从执行级控制的核心移除，改用轻量双向视觉-语言交互来构建任务条件表示。该方案在消费级 GPU（RTX 4090）上实现了 32 Hz 的实时推理，显存占用小于 1 GB，参数仅 0.2B，同时在 LIBERO 基准上达到了 97.7% 的平均成功率，匹配甚至超越更大规模的 VLA 模型，为高效机器人操作提供了新的视角。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将传统的 $V \to L \to A$ 路径改为直接的 $V + L \to A$ 映射。语言指令被轻量级文本编码器（如 BERT）单独编码，与视觉特征通过紧凑的双向交叉注意力模块直接交互，然后并行解码为连续动作块。不再使用大语言模型作为感知和动作之间的中心接口。  
- **关键技术细节**：
  1. **多模态特征编码**：
     - 视觉观测：使用 DINOv3 视觉编码器提取特征，通过投影层映射到共享隐藏维度 $d=256$，并添加位置嵌入和相机视角嵌入。
     - 语言指令：使用轻量级文本编码器（BERT）提取 token 级特征，保留完整 token 序列（不池化），以保留对象、属性、空间关系等信息。
     - 机器人状态：通过轻量投影网络独立编码，仅在动作解码阶段引入，不影响视觉-语言交互。
  2. **视觉-语言交互模块**：
     - 采用 $N=6$ 层双向交叉注意力，每层包含层归一化、视觉→指令交叉注意力、指令→视觉交叉注意力、以及模态特定的前馈网络（带残差连接）。
     - 输出为更新后的视觉特征和指令特征的拼接，形成“动作就绪”的多模态表示。
  3. **连续动作块预测**：
     - 使用 ACT 风格的轻量 transformer 解码器（$D_\theta$），以学习到的 $H$ 个动作查询为输入，结合多模态特征和机器人状态，并行预测 $H$ 步连续动作块。
     - 训练使用行为克隆+$\ell_1$ 损失，无需额外的语言建模目标。
- **公式说明**（文字描述）：
  - 视觉特征 $Z_{v}^{(i)} = P_v(f_{img}(I^{(i)})) + E_{pos}^{(i)} + e_{view}^{(i)}$，多相机拼接。
  - 指令特征 $Z_l = P_l(f_{text}(x)) \in \mathbb{R}^{N_l \times d}$。
  - 通过 $N$ 层 FusionLayer 双向更新：$(V^\ell, L^\ell) = \text{FusionLayer}^\ell(V^{\ell-1}, L^{\ell-1})$。
  - 动作输出 $\hat{A}_n = D_\theta(Q_a, [Z_n^{vl}; Z_n^s]) \in \mathbb{R}^{H \times d_a}$。

### 3. 实验设计：数据集、基准、对比方法

- **数据集与基准**：
  - **LIBERO**（单臂操作）：四个套件（Spatial, Object, Goal, Long），每个10个任务，共40个任务。使用改进的 no-noops RLDS 数据集。
  - **RoboTwin 2.0**（双臂操作）：50个语言条件双臂操作任务，仅使用官方清洁演示数据。
  - **真实世界评估**：AgileX Piper 机器人平台，4个代表性任务（抓取滚筒、移走扑克牌、按压订书机、叠放三个碗）。
- **对比方法**：
  - 非 VLA 基线：Diffusion Policy。
  - 能力导向 VLA（大型）：OpenVLA, π0, π0.5, CogVLA, Mantis, MM-ACT, VLA-JEPA, VEGA-3D 等。
  - 加速导向 VLA：OpenVLA-OFT, DDVLA。
  - 轻量 VLA：SmolVLA, DreamVLA, VLA-Adapter, Evo-1, StarVLA-α, UP-VLA 等。
- **评估指标**：任务成功率、参数量、推理延迟（从多模态输入到产生动作块的时间）、推理显存峰值。所有效率指标在单张 RTX 4090 上以 batch size 1 测量。

### 4. 资源与算力

- **训练配置**：使用 **4 块 RTX 4090 GPU**。
- **训练时长**：文中未明确给出总训练小时数，但给出了训练步数：
  - LIBERO：80k 步（含 10k 预热步），有效 batch size 7256。
  - RoboTwin 2.0：55k 步（含 1k 预热步），有效 batch size 192。
  - 真实世界微调：12.5k 步（4×65 演示数据）。
- **推理资源**：单张 RTX 4090，推理 VRAM <1 GB，延迟 31.2 ms（LIBERO）。RoboTwin 2.0 上延迟 43.4 ms。

### 5. 实验数量与充分性

- **实验数量**：包含大量实验：
  - LIBERO：2000次 rollout（50次×40任务），对比多种先进方法。
  - RoboTwin 2.0：5000次 rollout（100次×50任务），对比多个基线。
  - 真实世界：每任务40次 trial（共160次）。
  - 消融实验：语言条件 vs 无语言 vs 任务ID嵌入、不同文本编码器、不同交互设计（无交互、单向注意力、双向交互）、交互深度 N（2/4/6/8）、动作块大小 H（8/10/12/15）。
- **充分性与公平性**：
  - 对比方法均使用官方架构、实现和检查点，在同一硬件上测量延迟和显存。
  - 采用 VLA-Adapter rollout 协议（LIBERO）和 StarVLA 评估框架（RoboTwin 2.0），未引入额外技巧。
  - 消融实验控制变量，设计合理，覆盖了关键设计选择。
  - 结论：实验充分，公平性良好，验证了方法的有效性和效率优势。

### 6. 论文的主要结论与发现

- TurboVLA 证明了“执行级控制不必以 LLM 为中心”的假设，通过直接 $V+L \to A$ 映射，实现了性能与效率的优异平衡。
- 在 LIBERO 上以 0.2B 参数、31.2 ms 延迟、0.9 GB 显存取得 97.7% 平均成功率，优于或持平于大部分更大模型（如 π0.5 的 96.9%，参数大 15 倍以上）。
- 在 RoboTwin 2.0 双臂基准上，以 0.4B 参数、43.4 ms 延迟取得 60.2% 平均成功率，超越 π0.5（57.0%, 95.6 ms）和 StarVLA-α（50.3%, 74.9 ms）。
- 在真实机器人平台上，TurboVLA 在 4 个任务上均优于 π0.5。
- 消融结果表明：语义语言条件是必要的；BERT 作为文本编码器效果最佳；双向交互优于单向或无交互；交互深度 N=6 为最优；动作块大小 H=12 为最优。

### 7. 优点

- **方法创新性**：彻底摒弃 LLM 作为中心接口，提出直接 $V+L \to A$ 范式，设计简洁有效，显著降低计算和内存开销。
- **效率突出**：仅需消费级 GPU（RTX 4090），显存小于 1 GB，延迟 31 ms（32 Hz），参数仅 0.2B，适合边缘设备部署。
- **性能强大**：在多个基准上匹配或超越需要多 GB 显存、数十亿参数的大模型，展示了“小模型大性能”的可能性。
- **实验全面**：覆盖单臂/双臂/真实世界，进行大量消融，对比方法丰富且公平。
- **开源友好**：代码已公开，有助于社区复现和进一步研究。

### 8. 不足与局限

- **语义理解有限**：TurboVLA 主要面向具体的执行级指令，不具备高层次的语义理解、开放域推理或任务规划能力。文中也指出这作为未来方向。
- **未使用大规模预训练**：方法未利用大规模机器人预训练数据（如 Open X-Embodiment），未来可探索结合以提升泛化性。
- **实验覆盖有限**：
  - LIBERO 任务种类相对固定，未在更复杂、多样化场景（如挑战性物体、动态环境）中验证。
  - 真实世界任务仅 4 个，且与 LIBERO 任务类似领域，泛化到全新任务的证据不足。
  - 未涉及长时域、多步骤组合任务或需要密集推理的场景。
- **无对比完全非 VLA 的轻量基线**：虽然对比了 Diffusion Policy 等，但未与纯视觉+指令（无语言编码）的基线充分比较（消融中有无语言条件对比）。
- **潜在偏差风险**：训练数据均为仿真和受控真实演示，可能无法直接迁移到大规模、多样化真实场景。未见领域随机化/泛化压力测试。
- **资源报告不完整**：未提供训练总时长、能耗等指标；仅提供推理侧显存/延迟。

（完）
